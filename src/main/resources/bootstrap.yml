spring:
  application:
    name: dummy-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

server:
  port: 8080

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761

logging:
  level:
    org:
      springframework: info
